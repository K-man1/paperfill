# Copy to .env on the server and fill in real values. NEVER commit .env.
#   cp .env.example .env && nano .env

# Port gunicorn binds to. On Nest run `nest get-port` and paste the number here.
PORT=8080

# Flask session signing key. Generate one:  python -c "import secrets;print(secrets.token_hex(32))"
SECRET_KEY=

# LLM key. Default is the free Hack Club AI proxy (no card needed).
HCAI_API_KEY=
# Optional overrides — leave unset to use the Hack Club proxy defaults.
# OPENAI_BASE_URL=https://ai.hackclub.com/proxy/v1
# OPENAI_MODEL=qwen/qwen3-32b

# One-DM handwriting GPU service (Modal). Optional — leave blank to disable
# handwriting rendering (the app falls back to typed text).
ONEDM_MODAL_URL=
ONEDM_AUTH_TOKEN=
