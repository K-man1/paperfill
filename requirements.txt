flask>=3.1,<4
authlib>=1.7,<2
openai>=3.1,<4
pymupdf>=1.28,<2
gunicorn>=26.0,<27
youtube-transcript-api>=1.2,<2
requests>=2.34,<3
pillow>=12.3,<13
# Handwriting font builder (in-app "your handwriting" feature). Needs the
# `potrace` system binary too (apt-get install potrace / brew install potrace).
fonttools>=4.63,<5
opencv-python-headless>=5.0,<6
numpy>=2.5,<3
